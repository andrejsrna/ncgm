import type { Metadata } from "next";
import { SITE_NAME } from "@/lib/site";

export const metadata: Metadata = {
  title: `DMCA | ${SITE_NAME}`,
  description:
    "How to submit a DMCA takedown notice or counter-notice for content hosted on njkmusic.com, plus where to send it.",
};
