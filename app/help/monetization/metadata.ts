import type { Metadata } from "next";
import { SITE_NAME } from "@/lib/site";

export const metadata: Metadata = {
  title: `Monetization | ${SITE_NAME}`,
  description:
    "How to keep monetization active while using NJK Music releases—platform checklists, Content ID workflow, and when to request whitelisting.",
};
